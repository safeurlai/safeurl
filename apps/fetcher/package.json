{
  "name": "@safeurl/fetcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --target bun",
    "start": "bun run dist/index.js",
    "test": "bun test",
    "test:smoke": "bun test src/fetcher.smoke.test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'Linting not configured yet'"
  },
  "dependencies": {
    "@safeurl/core": "workspace:*",
    "@safeurl/mastra": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.3",
    "typescript": "^5.9.3"
  }
}

